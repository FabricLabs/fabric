name: npm test
on:
  pull_request:
    branches:
    - '**'
  push:
    branches:
    - master
jobs:
  test:
    name: Run tests
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
        - macos-latest
        - ubuntu-latest
    steps:
    - name: Checkout branch
      uses: actions/checkout@v3
    - name: Install Node.js on ${{ matrix.os }}
      uses: actions/setup-node@v4
      with:
        node-version-file: '.nvmrc'
        cache: 'npm'
        cache-dependency-path: package-lock.json
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.8'
    - name: Install dependencies
      run: npm run report:install
    - name: Download and install bitcoind
      run: |
        if [ "${{ matrix.os }}" = "ubuntu-latest" ]; then
          wget https://bitcoin.org/bin/bitcoin-core-28.0/bitcoin-28.0-x86_64-linux-gnu.tar.gz
          tar -xzf bitcoin-28.0-x86_64-linux-gnu.tar.gz
          sudo mv bitcoin-28.0/bin/bitcoind /usr/local/bin/
        else
          brew install bitcoin
        fi
    - name: Install lightningd
      run: |
        if [ "${{ matrix.os }}" = "ubuntu-latest" ]; then
          sudo apt-get update
          sudo apt-get install -y build-essential autoconf automake libtool libtool-bin gettext git libsqlite3-dev python3 net-tools zlib1g-dev libgmp-dev
          pip3 install mako
          git clone https://github.com/ElementsProject/lightning.git
          cd lightning
          git checkout v24.02.1
          ./configure
          make
          sudo make install
        else
          brew install core-lightning
        fi
    - name: Generate coverage report
      run: npm run report:coverage
    - name: Send coverage report
      uses: codecov/codecov-action@v3.1.1
      with:
        directory: ./reports/
